syntax = "proto3";
package cosmos.msg.v1;

import "cosmos_proto/cosmos.proto";

option (cosmos_proto.declare_interface) = {
  name: "Msg"
  description: "The Msg interface indicates that a type can be used as a message in a transaction."
};
